interface:
  display_name: "Ninox Data Mapper"
  short_description: "Descubre estructuras Ninox en modo read-only"
  default_prompt: "Use $ninox-data-mapper to extend the read-only P0/P1 mapping workflow with evidence-backed local artifacts."
